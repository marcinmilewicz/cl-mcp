/**
 * Framework Analyzer Interface
 *
 * Defines the contract for framework-specific component analyzers.
 * Each analyzer takes a library path and produces a ComponentMetadataFile.
 */

export type { FrameworkAnalyzer, AnalyzerOptions } from "../types.js";
