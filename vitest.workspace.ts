// TODO(vitest): workspace file is deprecated; migrate to `test.projects` in a
// root vitest.config.ts when bumping to vitest v4. Two-package workspace today,
// so the churn is minor once the major lands.
export default ["packages/*"];
